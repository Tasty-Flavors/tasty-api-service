spring:
  application:
    name: tasty-api-service
  profiles:
    active: ${PROFILE_ACTIVE:dev}

jwt:
  secret: ba76e755f23bb406c6e33cb18916be32
  expiration: 3600000

application:
  feign:
    viacep-name: viaCepClient
    viacep-url: https://viacep.com.br/ws

server:
  address: 0.0.0.0
  port: 3000
  servlet:
    context-path: /tasty-api-service
